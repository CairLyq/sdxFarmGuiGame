/**
 * on 2014-12-25
 * by leo
 */
class RenderUtils
{
    public static setRenderRt(rt:egret.Rectangle):void
    {
        //egret.RenderFilter.getInstance().clearDrawArea();
        //egret.RenderFilter.getInstance().addDrawArea( rt );
    }

    public static renderWhole():void
    {
        //RenderUtils.setRenderRt( new egret.Rectangle( 0, 0, Lyrs.SW, Lyrs.SH ) );
    }
}