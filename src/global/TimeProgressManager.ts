/**
 * on 2015-01-06
 * by leo
 *//*


class TimeProgressManager
{
    public timeProgress:game.TimeProgress;

    constructor()
    {
        this.timeProgress = UIMgr.instance.getPanel(PanelName.TIME_PROGRESS);
    }

    public setTime(cur:number, total:number):void
    {
        this.timeProgress.update(cur, total);
    }

    //===================================================
    private static _instance:TimeProgressManager;

    public static getInstance():TimeProgressManager
    {
        if(this._instance)
        {
            return this._instance;
        }
        else
        {
            this._instance = new TimeProgressManager();
        }
        return this._instance;
    }
}*/
